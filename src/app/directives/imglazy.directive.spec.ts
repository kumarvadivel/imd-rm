import { ImglazyDirective } from './imglazy.directive';

describe('ImglazyDirective', () => {
  it('should create an instance', () => {
    const directive = new ImglazyDirective();
    expect(directive).toBeTruthy();
  });
});
